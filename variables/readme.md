# Go Variables

## Note
Go is case-sensitive.

## Alternative Declaration

```go
var name string = "Ankur"
var age int = 20
```

## Short Variable Declaration Operator

In Go, `:=` is called the short variable declaration operator. It is used to declare and initialize a variable at the same time, without explicitly writing the type.